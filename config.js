// DKY Jewelry configuration — edit these values to customize.
window.DKY_CONFIG = {
  BUSINESS_NAME: "DKY Jewelry",
  // Your real WhatsApp number (digits only, with country code, NO + or spaces).
  WHATSAPP_NUMBER: "19414650463",
  // Buy-back range (% of live spot price).
  BUYBACK_MIN_PCT: 0.90,
  BUYBACK_MAX_PCT: 0.92,
  // Fallback gold price (USD per troy ounce) used if the live API is unreachable.
  FALLBACK_USD_PER_OZ: 2650,
};
